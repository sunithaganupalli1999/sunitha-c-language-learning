#include <stdio.h>
int main()
{
    int employid[] = {114, 150, 375, 180};
    int salary[] = {1000, 25000, 28000, 30000};
    printf("the employid output will be: %d \t %d\n", employid[3], employid[2]);
    printf("the salary output will be: %d \t %d\n", salary[1], salary[0]);
    return 0;
}